Fixed the `NIST - Software CVE Search` job's transient-failure handling to retry on any `requests.exceptions.RequestException` rather than a fixed enumeration of transport errors that omitted other retryable failure modes. `HTTPError` responses (4xx/5xx) and JSON decode errors are handled separately and are not retried, since those indicate data/input errors rather than transient transport failures.
